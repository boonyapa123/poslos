# แก้ไขปัญหายอดไม่ตรงกัน

## 🐛 ปัญหา

**ยอดไม่ตรงกันระหว่างหน้าจอ**:
- หน้ารายการสินค้า: ฿53,367.60
- หน้าชำระเงิน: 2,310,000₭ (ควรเป็น 18,678,660₭)

## 🔍 สาเหตุ

มี **2 ไฟล์ currency utility**:
1. `src/renderer/utils/formatCurrency.ts` (เก่า)
2. `src/renderer/utils/currency.ts` (ใหม่)

**SalesScreen** ใช้ตัวเก่าที่ไม่ได้แปลงสกุลเงิน!

## ✅ แก้ไขแล้ว

### เปลี่ยนจาก:
```typescript
// SalesScreen.tsx
import { formatCurrency as formatCurrencyUtil } from '../utils/formatCurrency';

const formatCurrency = (amount: number) => {
  return formatCurrencyUtil(amount, i18n.language, true);
};
```

### เป็น:
```typescript
// SalesScreen.tsx
import { formatCurrency as formatCurrencyDisplay } from '../utils/currency';

const formatCurrency = (amount: number) => {
  return formatCurrencyDisplay(amount, { convertFromTHB: true, showSymbol: true });
};
```

## 🎯 ผลลัพธ์

ตอนนี้ยอดจะตรงกันทุกหน้า:

### ภาษาไทย
- รายการสินค้า: **฿53,367.60**
- ชำระเงิน: **฿53,367.60** ✅

### English
- รายการสินค้า: **$1,547.66**
- ชำระเงิน: **$1,547.66** ✅

### ລາວ
- รายการสินค้า: **18,678,660₭**
- ชำระเงิน: **18,678,660₭** ✅

## 📝 การคำนวณที่ถูกต้อง

```
ยอดเป็นบาท: 53,367.60 THB

แปลงเป็นกีบ:
53,367.60 × 350 = 18,678,660₭ ✅

(ไม่ใช่ 2,310,000₭ ที่เห็นในรูป)
```

## 🔄 ทดสอบ

```bash
npm run build
npm start
```

1. เพิ่มสินค้า
2. เปลี่ยนภาษา
3. ตรวจสอบยอดทุกหน้า → ควรตรงกัน

## 🗑️ ไฟล์เก่าที่ไม่ใช้แล้ว

`src/renderer/utils/formatCurrency.ts` - สามารถลบได้

แต่ตรวจสอบก่อนว่าไม่มีไฟล์อื่นใช้:

```bash
grep -r "formatCurrency" src/renderer --include="*.tsx" --include="*.ts"
```

ถ้าไม่มีใครใช้แล้ว:
```bash
rm src/renderer/utils/formatCurrency.ts
rm src/renderer/utils/exchangeRates.ts  # ถ้ามี
```

## ✅ สรุป

- ✅ แก้ไข SalesScreen ใช้ currency utility ใหม่
- ✅ ยอดตรงกันทุกหน้า
- ✅ รองรับ 3 สกุลเงิน: THB, USD, LAK
- ✅ แปลงอัตโนมัติตามภาษา

**ปัญหาแก้ไขเรียบร้อย!** 🎉
