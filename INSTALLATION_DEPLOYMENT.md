# คู่มือ Build และติดตั้งโปรแกรม POS

## ✅ สถานะปัจจุบัน

### ข้อมูลพร้อม Build
- ✅ **pos-template.db**: 92 MB
- ✅ **สินค้า**: 28,412 รายการ
- ✅ **หน่วยสินค้า**: 52,343 รายการ
- ✅ **ราคา**: 366,200 รายการ
- ✅ **ลูกค้า**: 5,537 ราย
- ✅ **พนักงาน**: 21 คน
- ✅ **สาขา**: 47 สาขา

### ข้อมูลจะไปกับโปรแกรมหรือไม่?
**✅ ใช่! ข้อมูลทั้งหมดจะไปกับโปรแกรม**

เมื่อผู้ใช้ติดตั้งโปรแกรมและเปิดครั้งแรก:
1. โปรแกรมจะ copy `pos-template.db` ไปที่ user data folder
2. ผู้ใช้จะได้ข้อมูลสินค้าครบทันที ไม่ต้อง import เอง
3. แต่ละเครื่องจะมี database แยกกัน

---

## 🔨 การ Build โปรแกรม

### ขั้นตอนที่ 1: เตรียมข้อมูล (ทำครั้งเดียว)

```bash
# 1. Import ข้อมูลจาก Excel (ถ้ายังไม่ได้ทำ)
npm run import-data

# 2. เตรียม template database
npm run prepare-template
```

**ผลลัพธ์**:
```
📦 Preparing template database for build...
📁 Source: /Users/xxx/Library/Application Support/poslos/pos.db
📁 Target: pos-template.db
✅ Template created: 91.53 MB
```

### ขั้นตอนที่ 2: Build โปรแกรม

#### สำหรับ macOS
```bash
npm run build:mac
```

**ผลลัพธ์**:
- `dist-electron/POS System-1.0.0-arm64.dmg` (Apple Silicon)
- `dist-electron/POS System-1.0.0-x64.dmg` (Intel Mac)
- ขนาดประมาณ: **150-200 MB** (รวมข้อมูล)

#### สำหรับ Windows
```bash
npm run build:win
```

**ผลลัพธ์**:
- `dist-electron/POS System Setup 1.0.0.exe`
- ขนาดประมาณ: **150-200 MB**

#### สำหรับ Linux
```bash
npm run build:linux
```

**ผลลัพธ์**:
- `dist-electron/POS System-1.0.0.AppImage`
- `dist-electron/pos-system_1.0.0_amd64.deb`
- `dist-electron/pos-system-1.0.0.x86_64.rpm`

#### Build ทุก Platform พร้อมกัน
```bash
npm run build:all
```

---

## 📦 ไฟล์ที่ได้จาก Build

### โครงสร้างไฟล์ใน Build

```
dist-electron/
├── mac/
│   └── POS System.app/
│       └── Contents/
│           └── Resources/
│               ├── pos-template.db          ← ข้อมูลสินค้า 92 MB
│               └── ส่งข้อมูลPOS.xlsx        ← ไฟล์ Excel ต้นฉบับ
│
├── POS System-1.0.0-arm64.dmg              ← ไฟล์ติดตั้ง Mac (Apple Silicon)
└── POS System-1.0.0-x64.dmg                ← ไฟล์ติดตั้ง Mac (Intel)
```

### ขนาดไฟล์โดยประมาณ

| Platform | ขนาดไฟล์ติดตั้ง | หมายเหตุ |
|----------|----------------|----------|
| macOS DMG | 150-200 MB | รวม Electron + ข้อมูล |
| Windows EXE | 150-200 MB | รวม Electron + ข้อมูล |
| Linux AppImage | 150-200 MB | รวม Electron + ข้อมูล |

---

## 💾 การติดตั้งและข้อมูล

### เมื่อผู้ใช้ติดตั้งโปรแกรม

#### 1. ติดตั้งโปรแกรม
- **macOS**: ลาก `POS System.app` ไปที่ Applications
- **Windows**: รัน `POS System Setup.exe`
- **Linux**: รัน `POS System.AppImage` หรือติดตั้ง `.deb`/`.rpm`

#### 2. เปิดโปรแกรมครั้งแรก

โปรแกรมจะทำงานดังนี้:

```typescript
// src/main/database-init.ts
async function initDatabase() {
  const dbPath = path.join(app.getPath('userData'), 'pos.db');
  
  // ตรวจสอบว่ามี database หรือยัง
  if (!fs.existsSync(dbPath)) {
    console.log('📦 Database not found, creating from template...');
    
    // Copy จาก template
    const templatePath = path.join(process.resourcesPath, 'pos-template.db');
    fs.copyFileSync(templatePath, dbPath);
    
    console.log('✅ Database created from template');
  }
}
```

**ผลลัพธ์**:
- Database ถูก copy ไปที่:
  - **macOS**: `~/Library/Application Support/poslos/pos.db`
  - **Windows**: `%APPDATA%/poslos/pos.db`
  - **Linux**: `~/.config/poslos/pos.db`
- ผู้ใช้ได้ข้อมูลสินค้าครบ 28,412 รายการทันที

#### 3. ข้อมูลแต่ละเครื่อง

**สำคัญ**: แต่ละเครื่องจะมี database **แยกกัน**

```
เครื่องที่ 1: ~/Library/Application Support/poslos/pos.db
เครื่องที่ 2: ~/Library/Application Support/poslos/pos.db
เครื่องที่ 3: ~/Library/Application Support/poslos/pos.db
```

- ข้อมูลสินค้าเริ่มต้นเหมือนกัน (จาก template)
- ธุรกรรมการขายแยกกัน
- ต้องใช้ **Sync** เพื่อรวมข้อมูล

---

## 🔄 การอัพเดทข้อมูลสินค้า

### กรณีที่ 1: อัพเดทข้อมูลใน Build ใหม่

```bash
# 1. แก้ไขไฟล์ Excel
# 2. Import ข้อมูลใหม่
npm run import-data

# 3. เตรียม template ใหม่
npm run prepare-template

# 4. Build ใหม่
npm run build:mac
```

### กรณีที่ 2: อัพเดทข้อมูลในเครื่องที่ติดตั้งแล้ว

**วิธีที่ 1: Sync จาก Server**
```typescript
// ดึงข้อมูลสินค้าล่าสุดจาก Server
await window.electron.ipcRenderer.invoke('sync:pullFromServer');
```

**วิธีที่ 2: Replace Database**
```bash
# Backup database เดิม
cp ~/Library/Application\ Support/poslos/pos.db ~/Desktop/pos-backup.db

# Copy database ใหม่
cp pos-template.db ~/Library/Application\ Support/poslos/pos.db
```

---

## 🚀 ขั้นตอนการ Deploy

### สำหรับ Developer

```bash
# 1. เตรียมข้อมูล
npm run import-data
npm run prepare-template

# 2. Build
npm run build:mac  # หรือ platform ที่ต้องการ

# 3. ทดสอบ
open dist-electron/mac/POS\ System.app

# 4. แจกจ่าย
# Upload ไฟล์ .dmg/.exe ไปยัง server หรือ cloud storage
```

### สำหรับผู้ใช้

```bash
# 1. Download ไฟล์ติดตั้ง
# 2. ติดตั้งโปรแกรม
# 3. เปิดโปรแกรม
# 4. ใช้งานได้ทันที (มีข้อมูลสินค้าครบ)
```

---

## 📋 Checklist ก่อน Build

- [ ] ข้อมูลใน Excel ถูกต้องครบถ้วน
- [ ] รัน `npm run import-data` สำเร็จ
- [ ] รัน `npm run prepare-template` สำเร็จ
- [ ] ไฟล์ `pos-template.db` มีขนาด ~92 MB
- [ ] ตรวจสอบข้อมูลใน template:
  ```bash
  sqlite3 pos-template.db "SELECT COUNT(*) FROM products;"
  ```
- [ ] ทดสอบโปรแกรมใน development mode:
  ```bash
  npm run dev
  ```
- [ ] อัพเดท version ใน `package.json`
- [ ] เตรียม icons ใน `build/` folder

---

## 🔍 การตรวจสอบหลัง Build

### ตรวจสอบว่าข้อมูลอยู่ใน Build

#### macOS
```bash
# Extract .dmg
hdiutil attach dist-electron/POS\ System-1.0.0-arm64.dmg

# ตรวจสอบไฟล์
ls -lh /Volumes/POS\ System/POS\ System.app/Contents/Resources/pos-template.db

# ตรวจสอบข้อมูล
sqlite3 /Volumes/POS\ System/POS\ System.app/Contents/Resources/pos-template.db \
  "SELECT COUNT(*) FROM products;"
```

#### Windows
```bash
# Extract .exe (ใช้ 7-Zip)
7z x "dist-electron/POS System Setup 1.0.0.exe" -o"extracted"

# ตรวจสอบไฟล์
dir extracted\resources\pos-template.db
```

---

## ⚠️ ข้อควรระวัง

### 1. ขนาดไฟล์
- ไฟล์ติดตั้งจะใหญ่ (~150-200 MB) เพราะรวมข้อมูล
- ถ้าต้องการลดขนาด: ลดจำนวนข้อมูลใน template

### 2. การอัพเดท
- ถ้า build ใหม่ ผู้ใช้ต้องติดตั้งทับ
- ข้อมูลเก่าจะไม่หาย (อยู่ใน user data folder)
- แต่ template ใหม่จะไม่ถูกใช้ (เพราะมี database อยู่แล้ว)

### 3. การ Sync
- แต่ละเครื่องมีข้อมูลแยกกัน
- ต้องตั้งค่า Server Sync เพื่อรวมข้อมูล
- ดูคู่มือใน `SERVER_SYNC_SETUP.md`

### 4. License และ Code Signing
- macOS: ต้อง sign ด้วย Apple Developer Certificate
- Windows: ควร sign ด้วย Code Signing Certificate
- ไม่ sign จะมี warning ตอนติดตั้ง

---

## 🎯 สรุป

### ✅ ข้อมูลจะไปกับโปรแกรม
- ไฟล์ `pos-template.db` (92 MB) จะถูก include ใน build
- ผู้ใช้เปิดโปรแกรมครั้งแรกจะได้ข้อมูลสินค้าครบ 28,412 รายการ
- ไม่ต้อง import ข้อมูลเอง

### ✅ พร้อม Build และติดตั้ง
```bash
# Build
npm run build:mac

# ได้ไฟล์
dist-electron/POS System-1.0.0-arm64.dmg  (150-200 MB)

# ติดตั้ง
ลาก POS System.app ไปที่ Applications

# เปิดใช้งาน
เปิดโปรแกรม → มีข้อมูลสินค้าพร้อมใช้ทันที
```

### 📦 ขนาดและเนื้อหา
- **ขนาดไฟล์ติดตั้ง**: 150-200 MB
- **รวม**: Electron + โปรแกรม + ข้อมูลสินค้า 92 MB
- **แต่ละเครื่อง**: มี database แยกกัน

### 🔄 การอัพเดท
- **อัพเดทโปรแกรม**: Build ใหม่และติดตั้งทับ
- **อัพเดทข้อมูล**: ใช้ Sync จาก Server
- **Reset ข้อมูล**: ลบ database ใน user data folder

---

## 📚 เอกสารที่เกี่ยวข้อง

- `BUILD_GUIDE.md` - คู่มือการ build โดยละเอียด
- `SERVER_SYNC_SETUP.md` - การตั้งค่า sync กับ server
- `FEATURES_GUIDE.md` - ฟีเจอร์ต่างๆ ของโปรแกรม
- `PROJECT_STRUCTURE.md` - โครงสร้างโปรเจค
