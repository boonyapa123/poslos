# คู่มือการใช้งานหลายสกุลเงิน (Multi-Currency)

## ✅ ฟีเจอร์ที่เพิ่มแล้ว

### การแสดงผลสกุลเงินตามภาษา

เมื่อเปลี่ยนภาษา ระบบจะแสดงราคาในสกุลเงินที่เหมาะสม:

| ภาษา | สกุลเงิน | สัญลักษณ์ | อัตราแลกเปลี่ยน |
|------|----------|-----------|----------------|
| ไทย (th) | บาท (THB) | ฿ | 1.0 (ฐาน) |
| English (en) | Dollar (USD) | $ | 0.029 (~1 THB = $0.029) |
| ລາວ (lo) | ກີບ (LAK) | ₭ | 350 (~1 THB = 350₭) |

---

## 🔧 การทำงาน

### 1. ข้อมูลเก็บเป็น THB
ข้อมูลทั้งหมดใน database เก็บเป็น **บาท (THB)** เสมอ

```typescript
// ใน database
grandTotal: 214.00  // THB
```

### 2. แสดงผลตามภาษา
เมื่อแสดงผล จะแปลงเป็นสกุลเงินปัจจุบัน

```typescript
// ภาษาไทย
฿214.00

// English
$6.21  (214 × 0.029)

// ລາວ
74,900₭  (214 × 350)
```

### 3. รับเงินและเงินทอน
ผู้ใช้ป้อนจำนวนเงินในสกุลปัจจุบัน แล้วแปลงกลับเป็น THB เก็บ

```typescript
// ผู้ใช้เลือกภาษาลาว
// แสดง: 74,900₭
// ผู้ใช้จ่าย: 100,000₭
// เงินทอน: 25,100₭

// เก็บใน database (แปลงกลับเป็น THB):
amountReceived: 285.71 THB  (100,000 ÷ 350)
change: 71.71 THB  (25,100 ÷ 350)
```

---

## 📝 ไฟล์ที่แก้ไข

### 1. **Locale Files** - เพิ่มข้อมูลสกุลเงิน

#### `src/i18n/locales/th.json`
```json
{
  "currency": {
    "symbol": "฿",
    "code": "THB",
    "name": "บาท",
    "rate": 1
  }
}
```

#### `src/i18n/locales/en.json`
```json
{
  "currency": {
    "symbol": "$",
    "code": "USD",
    "name": "Dollar",
    "rate": 0.029
  }
}
```

#### `src/i18n/locales/lo.json`
```json
{
  "currency": {
    "symbol": "₭",
    "code": "LAK",
    "name": "ກີບ",
    "rate": 350
  }
}
```

### 2. **Currency Utility** - ฟังก์ชันแปลงสกุลเงิน

**`src/renderer/utils/currency.ts`**

```typescript
// ฟังก์ชันหลัก
getCurrencySymbol()      // ได้ "฿", "$", "₭"
getCurrencyCode()        // ได้ "THB", "USD", "LAK"
getExchangeRate()        // ได้ 1, 0.029, 350

convertFromTHB(amount)   // แปลงจาก THB → สกุลปัจจุบัน
convertToTHB(amount)     // แปลงจาก สกุลปัจจุบัน → THB

formatCurrency(amount, options)  // จัดรูปแบบพร้อมสัญลักษณ์
```

### 3. **Payment Component** - หน้าจ่ายเงิน

**`src/renderer/components/Payment.tsx`**

เปลี่ยนจาก:
```typescript
// เดิม - hardcode บาท
<div>฿{grandTotal.toFixed(2)}</div>
```

เป็น:
```typescript
// ใหม่ - แสดงตามภาษา
<div>{formatCurrency(grandTotal, { convertFromTHB: true })}</div>
```

---

## 🎯 ตัวอย่างการใช้งาน

### ในหน้าจ่ายเงิน (Payment.tsx)

```typescript
import { formatCurrency, convertFromTHB, convertToTHB } from '../utils/currency';

// 1. แสดงยอดรวม
<div>{formatCurrency(grandTotal, { convertFromTHB: true })}</div>
// ไทย: ฿214.00
// English: $6.21
// ລາວ: 74,900₭

// 2. ปุ่มจำนวนเงินด่วน
<button onClick={() => handleQuickAmount(100)}>
  {formatCurrency(100, { convertFromTHB: true })}
</button>
// ไทย: ฿100
// English: $2.90
// ລາວ: 35,000₭

// 3. แสดงเงินทอน
<div>{formatCurrency(convertToTHB(change), { convertFromTHB: true })}</div>

// 4. บันทึกข้อมูล (แปลงกลับเป็น THB)
const receivedTHB = convertToTHB(received);
const changeTHB = convertToTHB(change);
```

### ในหน้าอื่นๆ

```typescript
// SalesScreen.tsx - แสดงราคาสินค้า
import { formatCurrency } from '../utils/currency';

<td>{formatCurrency(item.unitPrice, { convertFromTHB: true })}</td>
<td>{formatCurrency(item.lineTotal, { convertFromTHB: true })}</td>

// ยอดรวม
<div>{formatCurrency(subtotal, { convertFromTHB: true })}</div>
<div>{formatCurrency(grandTotal, { convertFromTHB: true })}</div>
```

---

## 🔄 การอัพเดทอัตราแลกเปลี่ยน

### วิธีที่ 1: แก้ไขไฟล์ Locale

แก้ไขไฟล์ `src/i18n/locales/*.json`:

```json
{
  "currency": {
    "rate": 0.030  // อัพเดทอัตราใหม่
  }
}
```

### วิธีที่ 2: ดึงจาก API (แนะนำ)

สร้าง service ดึงอัตราแลกเปลี่ยนแบบ real-time:

```typescript
// src/services/ExchangeRateService.ts
export async function updateExchangeRates() {
  const response = await fetch('https://api.exchangerate-api.com/v4/latest/THB');
  const data = await response.json();
  
  // อัพเดทอัตราใน i18n
  i18n.addResourceBundle('en', 'currency', {
    rate: data.rates.USD
  }, true, true);
  
  i18n.addResourceBundle('lo', 'currency', {
    rate: data.rates.LAK
  }, true, true);
}
```

---

## 📊 ตัวอย่างการทำงาน

### Scenario: ขายสินค้า 214 บาท

#### 1. ภาษาไทย
```
ยอดชำระ: ฿214.00
รับเงิน: ฿500.00
เงินทอน: ฿286.00
```

#### 2. English
```
Grand Total: $6.21
Amount Received: $14.50
Change: $8.29
```

#### 3. ລາວ
```
ຍອດຊຳລະ: 74,900₭
ຮັບເງິນ: 100,000₭
เงินทอน: 25,100₭
```

### ข้อมูลที่เก็บใน Database (เป็น THB ทั้งหมด)
```sql
INSERT INTO transactions (
  grandTotal,
  amountReceived,
  change
) VALUES (
  214.00,    -- THB
  285.71,    -- 100,000₭ ÷ 350 = 285.71 THB
  71.71      -- 25,100₭ ÷ 350 = 71.71 THB
);
```

---

## ⚠️ ข้อควรระวัง

### 1. ความแม่นยำของทศนิยม
```typescript
// ใช้ toFixed() เพื่อจำกัดทศนิยม
const amount = convertFromTHB(214);
console.log(amount.toFixed(2));  // "74900.00"
```

### 2. การปัดเศษ
```typescript
// ควรปัดเศษเมื่อแสดงผล
formatCurrency(214.567, { convertFromTHB: true, decimals: 2 })
// ไทย: ฿214.57
// ລາວ: 74,998.45₭
```

### 3. การแปลงกลับ
```typescript
// ต้องแปลงกลับเป็น THB ก่อนบันทึก
const displayAmount = 100000;  // ₭
const thbAmount = convertToTHB(displayAmount);  // 285.71 THB
// บันทึก thbAmount ลง database
```

### 4. แสดงทั้งสองสกุล (Optional)
```typescript
// แสดงสกุลปัจจุบันและ THB
<div>
  {formatCurrency(grandTotal, { convertFromTHB: true })}
  {getCurrencyCode() !== 'THB' && (
    <span className="text-sm text-gray-500">
      (฿{grandTotal.toFixed(2)})
    </span>
  )}
</div>

// ผลลัพธ์ (ภาษาลาว):
// 74,900₭ (฿214.00)
```

---

## 🎨 UI/UX Improvements

### แสดงสกุลเงินที่เลือก
```typescript
import { getCurrencyInfo } from '../utils/currency';

const currencyInfo = getCurrencyInfo();
// { symbol: "₭", code: "LAK", name: "ກີບ", rate: 350 }

<div className="text-sm text-gray-600">
  Currency: {currencyInfo.name} ({currencyInfo.code})
</div>
```

### แสดงอัตราแลกเปลี่ยน
```typescript
{getCurrencyCode() !== 'THB' && (
  <div className="text-xs text-gray-500">
    Exchange Rate: 1 THB = {getExchangeRate()} {getCurrencyCode()}
  </div>
)}
```

---

## 🚀 สรุป

### ✅ สิ่งที่ทำเสร็จแล้ว
- เพิ่มข้อมูลสกุลเงินใน locale files (th, en, lo)
- สร้าง currency utility functions
- แก้ไข Payment component ให้แสดงและคำนวณตามสกุลเงิน
- แปลงข้อมูลอัตโนมัติเมื่อเปลี่ยนภาษา

### 📝 การใช้งาน
1. เปลี่ยนภาษา → สกุลเงินเปลี่ยนตาม
2. ราคาแสดงในสกุลที่เลือก
3. ข้อมูลเก็บเป็น THB ใน database
4. รองรับ 3 สกุล: THB, USD, LAK

### 🔄 ต่อยอด
- เพิ่มสกุลเงินอื่นๆ ใน locale files
- ดึงอัตราแลกเปลี่ยนแบบ real-time จาก API
- แสดงประวัติอัตราแลกเปลี่ยน
- ให้ผู้ใช้เลือกสกุลเงินแยกจากภาษา

---

## 📚 เอกสารที่เกี่ยวข้อง

- `MULTILINGUAL_SETUP.md` - การตั้งค่าหลายภาษา
- `EXCHANGE_RATE_SETUP.md` - การตั้งค่าอัตราแลกเปลี่ยน
- `src/renderer/utils/currency.ts` - Currency utility functions
- `src/renderer/components/Payment.tsx` - Payment component
