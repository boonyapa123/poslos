# Quick Start - POS System

## 🚀 Build และติดตั้งโปรแกรม

### คำตอบสั้นๆ

**Q: สามารถ build โปรแกรมติดตั้งได้ไหม?**
✅ **ได้!**

**Q: ข้อมูลสินค้าจะไปด้วยไหม?**
✅ **ไปด้วย!** ข้อมูลทั้งหมด 28,412 รายการจะอยู่ในโปรแกรม

---

## 📦 Build โปรแกรม (3 คำสั่ง)

```bash
# 1. เตรียมข้อมูล
npm run prepare-template

# 2. Build (เลือก platform)
npm run build:mac     # macOS
npm run build:win     # Windows
npm run build:linux   # Linux

# 3. ได้ไฟล์ติดตั้ง
# dist-electron/POS System-1.0.0.dmg (150-200 MB)
```

---

## 💾 ข้อมูลที่ได้

### ใน Build
- ✅ สินค้า: 28,412 รายการ
- ✅ หน่วยสินค้า: 52,343 รายการ
- ✅ ราคา: 366,200 รายการ
- ✅ ลูกค้า: 5,537 ราย
- ✅ พนักงาน: 21 คน
- ✅ สาขา: 47 สาขา

### เมื่อติดตั้ง
1. ติดตั้งโปรแกรม
2. เปิดครั้งแรก → ข้อมูลพร้อมใช้ทันที
3. ไม่ต้อง import เอง

---

## 📍 ตำแหน่งข้อมูล

### ใน Build Package
```
POS System.app/
└── Contents/
    └── Resources/
        └── pos-template.db  ← ข้อมูล 92 MB
```

### หลังติดตั้ง (แต่ละเครื่อง)
- **macOS**: `~/Library/Application Support/poslos/pos.db`
- **Windows**: `%APPDATA%/poslos/pos.db`
- **Linux**: `~/.config/poslos/pos.db`

---

## 🔄 การทำงาน

```
1. ผู้ใช้ติดตั้งโปรแกรม
   ↓
2. เปิดโปรแกรมครั้งแรก
   ↓
3. โปรแกรม copy pos-template.db → user data folder
   ↓
4. ผู้ใช้ได้ข้อมูลสินค้าครบ 28,412 รายการ
   ↓
5. พร้อมใช้งานทันที
```

---

## ⚡ คำสั่งด่วน

### Development
```bash
npm run dev              # รันโปรแกรม
npm run import-data      # Import ข้อมูลจาก Excel
```

### Production
```bash
npm run prepare-template # เตรียม template
npm run build:mac        # Build macOS
npm run build:win        # Build Windows
npm run build:all        # Build ทุก platform
```

### ตรวจสอบข้อมูล
```bash
# ดูข้อมูลใน template
sqlite3 pos-template.db "SELECT COUNT(*) FROM products;"

# ดูขนาดไฟล์
ls -lh pos-template.db
```

---

## 📚 เอกสารเพิ่มเติม

- **`INSTALLATION_DEPLOYMENT.md`** - คู่มือ build และติดตั้งแบบละเอียด
- **`BUILD_GUIDE.md`** - ขั้นตอนการ build
- **`SERVER_SYNC_SETUP.md`** - ตั้งค่า sync กับ server
- **`FEATURES_GUIDE.md`** - ฟีเจอร์ต่างๆ

---

## ✅ สรุป

| คำถาม | คำตอบ |
|-------|-------|
| Build ได้ไหม? | ✅ ได้ |
| ข้อมูลไปด้วยไหม? | ✅ ไปด้วย (92 MB) |
| ติดตั้งง่ายไหม? | ✅ ง่าย (ลาก & วาง หรือ run installer) |
| ใช้งานได้ทันทีไหม? | ✅ ได้ทันที (มีข้อมูลครบ) |
| แต่ละเครื่องแยกกันไหม? | ✅ แยกกัน (ใช้ sync เพื่อรวม) |

**พร้อม build และแจกจ่ายได้เลย!** 🎉
