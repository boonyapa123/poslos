# 📝 บันทึกการทำงานของระบบ POS

## 📅 วันที่สร้าง: 17 พฤศจิกายน 2025

---

## 🎯 ภาพรวมระบบ

ระบบ POS (Point of Sale) แบบ Desktop Application สำหรับร้านค้าปลีก
- **Technology Stack**: Electron + React + TypeScript + SQLite
- **Architecture**: Offline-First with Online Sync
- **Multi-language**: ไทย 🇹🇭, English 🇬🇧, ລາວ 🇱🇦

---

## 🗄️ โครงสร้างข้อมูล

### **Local Database (SQLite)**
ตั้งอยู่ที่: `~/Library/Application Support/[AppName]/pos.db` (macOS)

**Tables หลัก:**
1. **products** - สินค้า (~28,000 รายการ)
2. **product_units** - หน่วยสินค้า (~52,000 รายการ)
3. **product_prices** - ราคาสินค้า (~367,000 รายการ)
4. **customers** - ลูกค้า (~5,500 ราย)
5. **employees** - พนักงาน (ขาย + บริการ)
6. **bank_accounts** - บัญชีธนาคาร
7. **transactions** - บิลขาย (COMPLETED, PARKED)
8. **transaction_items** - รายการสินค้าในบิล
9. **shifts** - กะการทำงาน
10. **branches** - สาขา
11. **categories** - หมวดหมู่สินค้า
12. **departments** - หมวดย่อย
13. **warehouses** - คลังสินค้า
14. **configurations** - การตั้งค่า
15. **sync_logs** - Log การ sync

---

## 🔄 ระบบ Sync ข้อมูล

### **1. การดึงข้อมูล (Pull/Download)**

**Endpoint:** `GET /api/{resource}`
- `/api/products` - ดึงสินค้า + หน่วย + ราคา
- `/api/customers` - ดึงลูกค้า
- `/api/employees` - ดึงพนักงาน
- `/api/banks` - ดึงบัญชีธนาคาร

**Response Format (JSON):**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "sku": "P001",
      "name": "ปูนซีเมนต์",
      "units": [...],
      "prices": [...]
    }
  ]
}
```

**การทำงาน:**
1. User คลิก "⬇ ดึงข้อมูล"
2. APIClient ส่ง HTTP GET ไปยัง Server
3. Server ตอบกลับด้วย JSON
4. SyncManager บันทึกลง SQLite ด้วย `upsert()`
5. แสดงผลลัพธ์

---

### **2. การส่งข้อมูล (Push/Upload)**

**Endpoint:** `POST /api/sync/sales`

**Request Format (ตามโครงสร้าง Excel ชีท 14-15):**

**DOCINFO (Transaction Headers):**
```json
{
  "docInfo": [
    {
      "DI_DATE": 45974,              // Excel date number
      "DI_BRANCH": "POS-01",         // Terminal ID
      "DI_REF": "TXN-1234567890",    // Transaction number
      "DI_CRE_BY": "E001",           // Sales employee code
      "DI_AMOUNT": 15000,            // Grand total in satang (150.00 * 100)
      "DI_PM_BY": "Cash",            // Payment method (Cash/BANK/TRANSFER)
      "DI_Ccy": "K",                 // Currency (K = THB)
      "DI_BANK": 104,                // Bank ID (if transfer)
      "DI_DATE_TIME": "2025-11-17T10:30:00.000Z"
    }
  ]
}
```

**SKUMOVE (Transaction Items):**
```json
{
  "skuMove": [
    {
      "SKM_DATE": 45974,             // Excel date number
      "SKM_BCH": "POS-01",           // Branch code
      "DI_REF": "TXN-1234567890",    // Transaction number (link to DOCINFO)
      "SKM_No": 1,                   // Line number
      "SKU_CODE": "P001",            // Product SKU
      "GOODS_CODE": "8850123456789", // Unit barcode
      "UTQ_NAME": "ถุง",              // Unit name
      "UTQ_QTY": 1,                  // Conversion rate
      "QTY": 1,                      // Quantity
      "SKM_PRC": 15000,              // Unit price in satang (150.00 * 100)
      "SKM_AMOUNT": 15000,           // Line total in satang
      "SKM_Ccy": "K",                // Currency
      "WL_KEY": 1,                   // Warehouse ID
      "AR_CODE": "C001",             // Customer code
      "ARPRB_KEY": 1,                // Price level
      "CRE_BY": "E001",              // Sales employee
      "SV_BY": "E002",               // Service employee
      "SKM_DATE_TIME": "2025-11-17T10:30:00.000Z"
    }
  ]
}
```

**การแปลงข้อมูล:**
- วันที่ → Excel date number (days since 1900-01-01)
- เงิน → satang (x100)
- แยก header (DOCINFO) และ items (SKUMOVE)

**การทำงาน:**
1. Query transactions ที่ `isSynced = false`
2. แปลงเป็น DOCINFO + SKUMOVE format
3. POST ไปยัง `/api/sync/sales`
4. Mark transactions เป็น `isSynced = true`
5. แสดงผลลัพธ์

---

## 🕐 ระบบจัดการกะ (Shift Management)

### **การเปิดกะ:**
1. User คลิกปุ่ม 🕐 (Shift)
2. ใส่เงินสดเปิดกะ (Opening Cash)
3. ระบบสร้าง Shift record:
   - `shiftNumber`: SHIFT-{timestamp}
   - `terminalId`: จาก configuration
   - `openingCash`: จำนวนเงินที่ใส่
   - `status`: OPEN

### **การปิดกะ + Auto Sync:**

**Flow การทำงาน:**
```
1. User คลิก "ปิดกะ"
   ↓
2. ยืนยันการปิดกะ
   ↓
3. ปิดกะ (status = CLOSED)
   ├─ คำนวณ totalSales จาก transactions
   ├─ บันทึก closingCash
   └─ บันทึก endTime
   ↓
4. ตรวจสอบบิลที่ยังไม่ส่ง (isSynced = false)
   ↓
5. ถ้ามีบิลค้าง → ลอง Auto Sync
   ├─ Sync สำเร็จ ✅
   │  └─ แสดง: "ปิดกะเรียบร้อย + ส่งข้อมูล X บิล"
   │
   └─ Sync ล้มเหลว ❌ (เน็ตขัดข้อง)
      └─ แสดง: "ปิดกะเรียบร้อย + กรุณาส่งข้อมูลภายหลัง"
   ↓
6. ปิด Modal
```

**ข้อดี:**
- ✅ Auto Sync เมื่อปิดกะ (ไม่ต้องจำ)
- ✅ Graceful degradation (เน็ตขัดข้องก็ปิดกะได้)
- ✅ Manual Sync ยังใช้งานได้ (ปุ่ม "ส่งข้อมูลขาย")
- ✅ Visual indicator (แสดง "ยังไม่ส่ง: X บิล")

---

## 💰 ระบบชำระเงิน

### **Payment Methods:**
1. **เงินสด (CASH)**
   - รับจำนวนเงิน
   - คำนวณเงินทอนอัตโนมัติ
   
2. **โอน (TRANSFER)**
   - บันทึกเป็น BANK/TRANSFER
   - ไม่มีการ integrate กับระบบชำระเงินจริง

### **การทำงาน:**
1. User เลือก payment method
2. ใส่จำนวนเงิน (ถ้าเป็นเงินสด)
3. เลือกพิมพ์ใบเสร็จหรือไม่
4. ระบบบันทึก transaction:
   - `status`: COMPLETED
   - `isSynced`: false
   - `paymentMethod`: CASH/TRANSFER
5. แสดง "ขอบคุณ" บนหน้าจอลูกค้า
6. พิมพ์ใบเสร็จ (ถ้าเลือก)

---

## 📋 ระบบพักบิล (Park Bill)

### **การพักบิล:**
1. User คลิก "💾 พักบิล"
2. ระบบบันทึก transaction:
   - `status`: PARKED
   - `transactionNumber`: PARK-{timestamp}
3. Clear หน้าจอขาย

### **การเรียกบิลคืน:**
1. User กด F9 หรือคลิก "📋 บิลที่พักไว้"
2. เลือกบิลที่ต้องการ
3. ระบบโหลดบิลกลับมา
4. แก้ไขหรือชำระเงินต่อได้

---

## ⌨️ Keyboard Shortcuts

| Key | Function | Status |
|-----|----------|--------|
| **F6** | ค้นหาสินค้า | ✅ |
| **Delete** | เลือกลูกค้า | ✅ |
| **F9** | ดูบิลที่พักไว้ | ✅ |
| **End** | ชำระเงิน | ✅ |
| **M** | ลบรายการล่าสุด | ✅ |
| **ตัวเลข + *** | กำหนดจำนวน | ✅ |
| **Enter** | เพิ่มสินค้า | ✅ |

---

## 🖥️ Customer Display (หน้าจอลูกค้า)

### **การทำงาน:**
- แสดงบน secondary window (dual screen)
- อัพเดท real-time เมื่อมีการเปลี่ยนแปลง
- แสดง: รายการสินค้า, ราคา, ยอดรวม, VAT
- แสดง "ขอบคุณ" 5 วินาทีหลังชำระเงิน

### **IPC Communication:**
```javascript
// Main Window → Customer Display
ipcRenderer.send('customer-display:update', displayData);
ipcRenderer.send('customer-display:show-thank-you');
ipcRenderer.send('customer-display:show-qr', qrCodeData);
```

---

## 🌐 ระบบหลายภาษา (i18n)

### **ภาษาที่รองรับ:**
- 🇹🇭 ไทย (th)
- 🇬🇧 English (en)
- 🇱🇦 ລາວ (lo)

### **การเปลี่ยนภาษา:**
- คลิกธงชาติที่ Header
- บันทึกใน localStorage
- เปลี่ยนทันทีทั้งระบบ

### **Translation Files:**
- `src/i18n/locales/th.json`
- `src/i18n/locales/en.json`
- `src/i18n/locales/lo.json`

---

## 📊 การ Import ข้อมูลจาก Excel

### **ไฟล์:** `ส่งข้อมูลPOS.xlsx`

### **ชีทที่ Import (15 ชีท):**
1. BRANCH - สาขา
2. ICCAT - หมวดหมู่หลัก
3. ICDEPT - หมวดย่อย (~12,000 รายการ)
4. WARELOCATION - คลังสินค้า
5. UOFQTY - หน่วยนับ
6. SKUMASTER - สินค้า (~28,000 รายการ)
7. GOODSMASTER - รหัสซื้อขาย (~52,000 รายการ)
8. ARPRB - ตารางราคา
9. ARFILE - ลูกค้า (~5,500 ราย)
10. ARPLU - ราคาสินค้า (~367,000 รายการ)
11. USER - พนักงานขาย
12. SERVICE - พนักงานบริการ
13. BANK - บัญชีธนาคาร
14. DOCINFO - ตัวอย่าง transaction headers
15. SKUMOVE - ตัวอย่าง transaction items

### **การทำงาน:**
- Import ครั้งแรกเมื่อเปิดโปรแกรม (ถ้า database ว่าง)
- ใช้ individual insert (ไม่ใช่ bulk insert)
- ข้ามรายการที่ซ้ำ (duplicate)
- แสดง progress ระหว่าง import

---

## 🔧 Configuration (Settings)

### **การตั้งค่าที่สำคัญ:**
1. **Terminal ID** - รหัสเครื่อง POS
2. **API Base URL** - URL ของ server
3. **API Key** - รหัสสำหรับ authentication
4. **VAT Rate** - อัตราภาษี (%)
5. **VAT Type** - INCLUSIVE/EXCLUSIVE
6. **Language** - ภาษาที่ใช้

### **การเก็บ:**
- บันทึกใน `configurations` table
- Key-Value pairs
- Load เมื่อเปิดโปรแกรม

---

## 🚨 Error Handling

### **Sync Errors:**
- ✅ ไม่ block การทำงานหลัก
- ✅ แสดงข้อความที่เข้าใจง่าย
- ✅ เก็บข้อมูลไว้ sync ภายหลัง
- ✅ Visual indicator (ยังไม่ส่ง: X บิล)

### **Network Errors:**
- ✅ Offline-first architecture
- ✅ ทำงานได้โดยไม่ต้องต่อ internet
- ✅ Sync เมื่อเน็ตกลับมา

### **Database Errors:**
- ✅ Try-catch ทุก operation
- ✅ Log errors ลง console
- ✅ แสดง user-friendly messages

---

## 📈 Performance Optimization

### **Database:**
- ✅ Indexes บน frequently queried columns
- ✅ Individual insert แทน bulk (หลีกเลี่ยง SQL too large)
- ✅ Upsert แทน insert/update แยก

### **UI:**
- ✅ Debouncing สำหรับ search (300ms)
- ✅ Keyboard navigation
- ✅ Real-time updates

---

## 🔐 Security

### **Authentication:**
- API Key ใน headers
- Terminal ID tracking
- No sensitive data in localStorage

### **Data:**
- Local SQLite (encrypted ถ้าต้องการ)
- HTTPS สำหรับ API calls
- No credit card processing

---

## 📝 Development Notes

### **สิ่งที่ทำเสร็จ:**
- ✅ ระบบขายสินค้าครบถ้วน
- ✅ Customer Display
- ✅ Sync ข้อมูล (Pull/Push)
- ✅ จัดการกะ + Auto Sync
- ✅ พักบิล/เรียกบิลคืน
- ✅ ชำระเงิน (เงินสด/โอน)
- ✅ หลายภาษา (3 ภาษา)
- ✅ Keyboard shortcuts
- ✅ Settings
- ✅ Import จาก Excel

### **สิ่งที่ยังไม่ได้ทำ:**
- ⏳ Printer integration (มีโครงสร้างพื้นฐาน)
- ⏳ First-time setup wizard
- ⏳ Report/Dashboard
- ⏳ Inventory management
- ⏳ Advanced hotkeys (End+M, F10, etc.)

---

## 🎯 Best Practices

### **การใช้งาน:**
1. เปิดกะก่อนขาย
2. Sync ข้อมูลเป็นระยะ
3. ปิดกะเมื่อเลิกงาน (จะ auto sync)
4. ตรวจสอบ "ยังไม่ส่ง" เป็นประจำ

### **การบำรุงรักษา:**
1. Backup database เป็นประจำ
2. ตรวจสอบ sync logs
3. อัพเดทข้อมูลสินค้าจาก server
4. ทดสอบการเชื่อมต่อ

---

## 📞 Support Information

### **Log Files:**
- Console logs: Developer Tools (F12)
- Sync logs: `sync_logs` table
- Database: `pos.db`

### **Troubleshooting:**
1. ไม่พบสินค้า → Sync ดึงข้อมูล
2. ไม่สามารถ sync → ตรวจสอบ Settings
3. หน้าจอลูกค้าไม่แสดง → Restart โปรแกรม
4. Import ใหม่ → ลบ `pos.db` แล้ว restart

---

## 📅 Version History

### **v1.0.0 (17 Nov 2025)**
- Initial release
- ระบบขายสินค้าครบถ้วน
- Sync ข้อมูล (DOCINFO + SKUMOVE format)
- Auto Sync เมื่อปิดกะ
- หลายภาษา (ไทย, English, ລາວ)

---

**สร้างโดย:** Kiro AI Assistant  
**วันที่:** 17 พฤศจิกายน 2025  
**สถานะ:** Production Ready ✅
